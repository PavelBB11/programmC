Заголовочные файлы
Программа подключает необходимые заголовочные файлы с помощью директивы #include. Эти заголовочные файлы предоставляют важные функции и макросы для ввода/вывода файлов и операций, связанных со временем.
Основная функция
Функция main является точкой входа программы, где начинается выполнение.
Измерение времени
Программа использует тип данных clock_t и функцию clock() из заголовочного файла <time.h> для измерения использованного процессорного времени.
Ввод из файла
Программа предлагает пользователю ввести количество целых чисел, затем генерирует такое количество случайных чисел от 0 до 99. Эти числа записываются в файл с именем "input.txt".
Вывод в файл
Программа считывает числа из "input.txt", выполняет некоторые вычисления на основе этих чисел и записывает результаты в "output.txt".
Операции с матрицами
Программа считывает числа из "input.txt" в двумерный массив (матрицу), затем выполняет операции для отображения матрицы, вычисления суммы каждой строки и вычисления суммы каждого столбца.
Вычисление времени
Наконец, программа вычисляет и отображает общее использованное процессорное время.

main()
Это главная функция программы, в которой начинается выполнение программы. Она содержит весь основной код программы.
clock()
Функция clock() измеряет процессорное время, которое затрачивается на выполнение программы.
printf()
Функция printf() используется для вывода форматированной строки на экран.
scanf()
Функция scanf() используется для считывания ввода с клавиатуры.
fopen()
Функция fopen() открывает файл для операций ввода/вывода.
fprintf()
Функция fprintf() записывает форматированные данные в файл.
fclose()
Функция fclose() закрывает файл, который был открыт ранее.
rand()
Функция rand() генерирует псевдослучайное число.
srand()
Функция srand() инициализирует генератор псевдослучайных чисел.
while()
Цикл while выполняет определенный блок кода до тех пор, пока указанное условие истинно.
fscanf()
Функция fscanf() считывает форматированные данные из файла.
clock_t
clock_t - это тип данных, используемый для измерения процессорного времени.



Вычисление суммы по строкам:
Для каждой строки матрицы:
Создается переменная rowSum и инициализируется значением 0.
Вложенный цикл проходит по каждому элементу в строке и добавляет его значение к переменной rowSum.
После завершения вложенного цикла, выводится сообщение о сумме строки в формате "Сумма строки %d: %d", где %d - номер строки и вычисленная сумма для этой строки.
Вычисление суммы по столбцам:
Для каждого столбца матрицы:
Создается переменная colSum и инициализируется значением 0.
Вложенный цикл проходит по каждому элементу в столбце и добавляет его значение к переменной colSum.
После завершения вложенного цикла, выводится сообщение о сумме столбца в формате "Сумма столбца %d: %d", где %d - номер столбца и вычисленная сумма для этого столбца.
